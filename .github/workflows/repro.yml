name: repro
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: my-custom-runner          # ← NOT in runnerLabels
    steps:
      - uses: actions/checkout@v4
      - run: echo "build"
  publish:
    runs-on: [self-hosted, linux, x64] # ← also not in runnerLabels
    steps:
      - uses: actions/checkout@v4
      - run: echo "publish"
